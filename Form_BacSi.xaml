<Window x:Class="QUANLYBENHVIEN.Form_BacSi"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:QUANLYBENHVIEN"
        mc:Ignorable="d"
        Title="Form_BacSi" Height="450" Width="400">
    <Grid>
        <GroupBox Header="Thông tin bác sĩ" Grid.Column="0" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Margin="10" Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Grid.Row="0"  Content="Họ Tên: " Margin="5"/>
                    <Label Grid.Column="0" Grid.Row="1"  Content="Chuyên khoa: " Margin="5"/>
                    <Label Grid.Column="0" Grid.Row="2"  Content="Số điện thoại: " Margin="5"/>
                    <Label Grid.Column="0" Grid.Row="3"  Content="Email: " Margin="5"/>
                    <Label Grid.Column="0" Grid.Row="4"  Content="Ảnh đại diện: " Margin="5"/>

                    <TextBox x:Name="Tb_hoTen" Grid.Column="1" Grid.Row="0" Width="auto" Margin="5"/>
                    <ComboBox x:Name="ComboBox_chuyenKhoa" Grid.Column="1" Grid.Row="1" Margin="5">
                        <ComboBoxItem Content="Tim Mạch" IsSelected="True"/>
                        <ComboBoxItem Content="Thần Kinh"/>
                        <ComboBoxItem Content="Xương Khớp"/>
                        <ComboBoxItem Content="Xét Nghiệm"/>
                        <ComboBoxItem Content="Nhi Khoa"/>
                        <ComboBoxItem Content="Sản Phụ Khoa"/>
                        <ComboBoxItem Content="Mắt"/>
                        <ComboBoxItem Content="Hàm Răng Mặt"/>
                    </ComboBox>
                    <TextBox x:Name="Tb_soDienThoai" Grid.Column="1" Grid.Row="2" Margin="5"/>
                    <TextBox x:Name="Tb_email" Grid.Column="1" Grid.Row="3" Margin="5"/>
                    <StackPanel Grid.Column="1" Grid.Row="4" Orientation="Horizontal" Margin="5">
                        <!-- Ảnh hiển thị -->
                        <Image x:Name="imgAvatar" Width="100" Height="100" Stretch="UniformToFill" Margin="0,0,10,0" />

                        <!-- Nút để chọn ảnh -->
                        <Button Content="Chọn ảnh" Width="100" Click="Button_ChonAnh_Click" Height="30"/>
                    </StackPanel>

                </Grid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">

                    <Button Content="Xác nhận" Width="100" Height="25" Margin="10" Click="Button_XacNhan_Click"/>

                    <Button Content="Hủy" Width="100" Height="25" Margin="10" Click="Button_Huy_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
