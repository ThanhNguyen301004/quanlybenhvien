<Page x:Class="QUANLYBENHVIEN.ThongKeDoanhThu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QUANLYBENHVIEN"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ThongKeDoanhThu">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label FontWeight="Bold" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 50 0 0" Content="THỐNG KÊ DOANH THU"/>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="" Grid.Column="0" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Margin="10" Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Label Grid.Column="0" Grid.Row="0"  Content="Từ ngày: " Margin="5"/>
                            <Label Grid.Column="0" Grid.Row="1"  Content="Đến ngày: " Margin="5"/>
                            <DatePicker x:Name="dp_tuNgay" Grid.Column="1" Grid.Row="0" Margin="5" SelectedDate="{x:Static sys:DateTime.Now}"/>
                            <DatePicker x:Name="dp_denNgay" Grid.Column="1" Grid.Row="1" Margin="5" SelectedDate="{x:Static sys:DateTime.Now}"/>

                        </Grid>
                        <Button Click="Button_ThongKe_Click" Grid.Row="1" Content="Thống kê" Margin="15" Height="25"/>
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Label Grid.Column="0" Grid.Row="0"  Content="Tổng số ca: " Margin="5"/>
                            <Label Grid.Column="0" Grid.Row="1"  Content="Tổng doanh thu: " Margin="5"/>
                            <Label x:Name="Lb_soCa" Grid.Column="1" Grid.Row="0" Content="0" Margin="5"/>
                            <Label x:Name="Lb_tongDoanhThu" Grid.Column="1" Grid.Row="1" Content="0 VND" Margin="5"/>

                        </Grid>
                        <Button Click="Button_LamMoi_Click" Grid.Row="3" Content="Làm mới" Margin="15" Height="25"/>

                    </Grid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                    </StackPanel>

                </Grid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Bảng dữ liệu" Margin="5">
                <DataGrid IsReadOnly="True" AutoGenerateColumns="False" x:Name="DG_HOADONVIENPHI" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Hóa Đơn" Binding="{Binding MaHoaDon}" Width="*"/>
                        <DataGridTextColumn Header="Bệnh Nhân" Binding="{Binding BENHNHAN.HoTen}" Width="*"/>
                        <DataGridTextColumn Header="Phòng Bệnh" Binding="{Binding PHONGBENH.SoPhong}" Width="*"/>
                        <DataGridTextColumn Header="Ngày Nhập Viện" Binding="{Binding NgayNhapVien}" Width="*"/>
                        <DataGridTextColumn Header="Ngày Xuất Viện" Binding="{Binding NgayXuatVien}" Width="*"/>
                        <DataGridTextColumn Header="Thành Tiền" Binding="{Binding ThanhTien}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>


    </Grid>
</Page>
